{
  "name": "@edonahue/now-building-control-plane",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "npm run build && npm test",
    "test": "vitest run"
  },
  "dependencies": {
    "@vercel/blob": "^2.0.0",
    "@vercel/connect": "^0.1.0",
    "openai": "^6.18.0",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^4.0.8"
  }
}
